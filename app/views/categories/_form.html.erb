<%= form_for(@category) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "Category name" %>   
  <%= f.submit %>  
<% end %>

<%= link_to "Cancel request and return to posts", posts_path %> 
